# v0.17.1

## 目标

修正 `obsidian` skill 的紧凑排版规则，把 Markdown 表格前后的必要空行明确列为例外，避免后续写 Obsidian 笔记时误删表格前空行导致表格无法渲染。

## 变更清单

- `skills/obsidian/SKILL.md`：`obsidian` skill 升级到 `0.1.2`，在空行规则中补充 Markdown 表格前后空行要求，并加入正确/错误示例。
- `.claude-plugin/plugin.json` / `.codex-plugin/plugin.json`：插件版本升级到 `0.17.1`。
- `CHANGELOG.md`：记录本次指令优化。

## 架构决策

这次变更不新增 skill、command、tool 或 manifest 关键词，只修正既有 Obsidian 写作规范的边界。表格空行保留在 `SKILL.md` 核心规则中，因为它属于高频 Markdown 渲染约束，不适合下沉到 references。

## 排除项及原因

- 不更新 README：能力清单没有新增或改名，README 中的 Obsidian 能力描述仍然成立。
- 不新增脚本：这是写作规则调整，不需要确定性转换工具。
