# v0.21.14 - generate-image 客户端中文注释补齐

## 目标

让 `generate-image` 客户端脚本符合仓库新增的代码注释规则，把近期补充的脚本结构说明统一调整为中文，并解释关键边界保护意图。

## 变更清单

- `skills/generate-image/scripts/generate_image_client.py`
  - 为生成结果数据结构、错误类型、客户端入口和各后端生成方法补充中文 docstring。
  - 为 API key 缓存路径、读取、写入、鉴权错误分流和输出路径去重逻辑补充中文说明。
  - 保留 `--save-api-key-stdin` / `--save-api-key-only` 的多行参数格式，提升 CLI 参数块可读性。
- `.claude-plugin/plugin.json` / `.codex-plugin/plugin.json`
  - 插件版本升级到 `0.21.14`。
- `CHANGELOG.md`
  - 新增 `0.21.14` 版本记录。

## 架构决策

- 本次是注释规范化和脚本可维护性改进，没有新增 skill 或用户可见能力，因此按 patch 版本发布。
- 注释集中解释接口边界、鉴权分流、密钥缓存和文件输出保护约束，不改变生成链路请求结构。
- 继续保持客户端只依赖 Python 标准库，避免为注释和格式整理引入新依赖。

## 排除项及原因

- 不提交 `outputs/` 下的生成图片。它们是本地生成产物，不属于插件源码或版本记录。
- 不升级 `generate-image` skill 版本。skill 触发和用户流程未变化。
- 不修改 README。插件能力描述没有变化。
