interface:
  display_name: "Xbuild Open Source"
  short_description: "自动补开并同步 EasyBox/xbuild 缺失源码仓"
  default_prompt: "Use $xbuild-open-source to locate and automatically open missing EasyBox/xbuild source repositories."
policy:
  allow_implicit_invocation: true
