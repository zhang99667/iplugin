---
name: delegated-search
version: 0.1.0
description: 通用广域检索委派助手。当用户提出“帮我找全相关实现/资料”“查一下这些字段从哪来”“这个链路可能哪里断”“对比多个来源”等需要横向搜索大量代码、文档、日志、配置、历史记录、网页资料或多条线索的任务时触发，不局限于代码任务。用于指导执行助手判断是否应把搜索与证据收集拆给 explorer/subagent，或在需要授权时先询问，主线程负责判断、整合和最终行动。
tags: [search, delegation, investigation, context]
---

# Delegated Search

## 目标

在广域检索、证据收集、链路定位、资料核验类任务中，减少主线程上下文消耗。主线程负责拆题、判断、验证和交付；可并行、可外包的搜索工作优先交给 explorer/subagent。

这个 skill 是通用插件能力，不绑定某个具体软件，也不只服务代码阅读。适用对象包括代码库、文档、日志、配置、历史记录、网页资料、数据文件和用户贴出的长材料。

## 触发判断

遇到以下情况时优先考虑使用本技能：

- 任务需要在多个目录、仓库、文档、日志、网页或数据源之间横向搜索。
- 用户给出的材料很长，需要先提取线索、字段、路径、实体、异常码或证据点。
- 问题可能有多条独立线索，例如“字段从哪里来”“链路在哪里断”“这些配置分别对应什么”“帮我找全相关实现”。
- 需要对比多个来源，而不是只读一两个明确文件。
- 主线程继续阅读会快速消耗大量上下文，但搜索任务可以独立完成。

不要求用户显式说“使用子 agent”才触发。是否委派由执行助手根据任务宽度、风险和当前工具能力判断；如果当前环境需要用户授权才能委派，应先用一句话询问。

## 不适用情况

- 用户只问一个明确文件、明确方法或明确字段，直接 `rg` 和少量阅读即可回答。
- 任务需要立即修改代码，且检索范围很小。
- 子 agent 不可用，或当前平台规则要求显式授权但用户尚未授权。
- 搜索结果必须由主线程实时交互判断，拆出去反而增加来回成本。

## 工作流

1. 先把用户问题拆成 2 到 5 个可独立验证的搜索问题。
2. 判断哪些问题适合委派 explorer/subagent：
   - 范围广、证据收集型、可并行。
   - 输出应是结论和证据，不是大段原文。
   - 不把最终判断完全交给子 agent。
3. 如果可委派，给 explorer/subagent 明确搜索边界、关键词、排除项和输出模板。
4. 主线程收到结果后，抽查关键证据，补齐遗漏，再形成最终判断。
5. 如果不能委派，使用紧凑本地检索：优先 `rg`/结构化查询，只读取命中文件的关键片段。

## 子 Agent 输出模板

要求 explorer/subagent 使用以下结构返回，避免堆原始日志：

```text
结论：
证据：
- 来源/路径/定位：摘要
- 来源/路径/定位：摘要
未覆盖：
下一步建议：
```

证据必须可追溯。代码证据给文件路径和行号；文档证据给标题、链接或段落定位；日志证据给关键字段、时间或请求标识。

## 主线程交付要求

最终回答要区分三类内容：

- 已确认事实：来自可追溯证据。
- 合理推断：说明推断依据和不确定性。
- 下一步排查：给出最短可执行动作，例如需要补哪条日志、查哪个字段、断点打在哪里、找哪个服务方确认。

不要把子 agent 的原始结果整段转述给用户。主线程要整合为清晰结论，并保留最关键的证据链。
