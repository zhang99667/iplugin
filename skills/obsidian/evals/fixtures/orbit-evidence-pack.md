# Orbit Plugin evidence pack

只允许使用以下事实；没有列出的判断必须删除、降级为待确认或明确标为示例假设。

- `plugin.json` 是必需文件，声明插件名和版本。
- Orbit 启动时依次扫描插件根目录、校验 manifest、注册当前产品支持的组件。
- Orbit Desktop 支持 hooks；Orbit Web 忽略 hooks。
- 被停用的插件不进入注册阶段。
- 没有证据支持“Desktop 更专业”“manifest 比目录更重要”或任何产品优劣排序。
